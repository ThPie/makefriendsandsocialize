interface BrandedLoaderProps {
  message?: string;
}

/**
 * Invisible loader — renders nothing, lets the page load naturally.
 */
export function BrandedLoader({ message }: BrandedLoaderProps) {
  return null;
}
